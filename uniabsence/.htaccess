Options -Indexes

RewriteEngine On

# اسمح للملفات الموجودة فعلاً بالمرور (HTML, CSS, JS, uploads)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# أعد توجيه كل شيء إلى index.php في جذر الموقع
RewriteRule ^(.*)$ index.php [QSA,L]
